import { STORE_ADD, BADGE_ADD, STORE_REMOVE, BADGE_REMOVE } from './../constants';

const initialState = {
  store: [],
  badgeCount: 0,
};

const storeReducer = (state = initialState, action) => {
  switch (action.type) {
    
  }
}

export storeReducer;
